name: ipcmail
version: 0.3.0
description: |
  Zero copy shared memory, pub/sub bus, unix socket and pipe mailboxes for Crystal, wired into the Crystal event loop.

authors:
  - shpeckman <geelenrobin@proton.me>

crystal: '>= 1.21.0'

license: MIT

targets:
  ipcmail:
    main: bin/ipcmail.cr